import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder — tests will be added as the app grows
  testWidgets('app smoke test', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}
